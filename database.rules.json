{
  "rules": {
    "worlds": {
      "$worldId": {
        "players": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && (auth.uid == $uid || auth.token.admin == true)"
          }
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
